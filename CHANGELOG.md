# μChordbot Workspace Changelog

## Logging Rule (Mandatory)

- ルート配下（`docs/`, `code/` を含む）でファイルを追加・更新・削除した場合は、このファイルに必ず追記する。
- 各ログには必ず日時（`YYYY-MM-DD HH:mm:ss ±TZ`）を含める。
- 各ログには最低限、`対象`、`変更内容`、`理由` を記載する。
- 仕様書の更新だけでなく、実装コード更新も同じルールで記録する。
- 1回の作業で複数ファイルを変更した場合は、まとまりごとに1エントリとして記録してよい。

---

## 2026-05-05 15:38:55 +09:00

- 対象: `docs/specs/00_overview.md`
- 変更内容: 仕様書の全体入口を作成し、`μChordbot` の目的・設計原則・非目標・仕様書マップを定義。
- 理由: 仕様群を実装前に読める全体概要が必要だったため。

- 対象: `docs/specs/01_requirements.md`
- 変更内容: 要件定義を追加し、微分音コード実験、PWA対応、内部音高、スナップ、丸め込み、コード/スケール/DSL連携の要件をID付きで整理。
- 理由: 何が必要かを先に固定し、実装判断の基準を明確にするため。

- 対象: `docs/specs/02_functional_spec.md`
- 変更内容: 数直線入力、スナップ、音高プリセット、コード保存、進行再生、I/O、DSL取込などの機能仕様を追加。
- 理由: 要件を具体的な機能単位へ分解するため。

- 対象: `docs/specs/03_behavior_spec.md`
- 変更内容: 音高正規化、スナップ、丸め込み、保存、進行再生、インポート統合などの挙動ルールを追加。
- 理由: 条件分岐と評価順を一意にするため。

- 対象: `docs/specs/04_data_spec.md`
- 変更内容: 音高プリセット、コード、スケール、進行、設定、I/Oフォーマットのデータ構造を追加。
- 理由: 保存形式と内部参照構造を固定するため。

- 対象: `docs/specs/05_ui_spec.md`
- 変更内容: 単一SPA構成、音高設計画面、進行画面、設定画面、各UI要素の表示要件を追加。
- 理由: 画面責務と操作導線を明文化するため。

- 対象: `docs/specs/06_implementation_spec.md`
- 変更内容: 実装責務分離、保存層、PWA、DSL取込、Phase構成などの実装方針を追加。
- 理由: 実装の進め方と責務境界を固定するため。

- 対象: `docs/specs/07_test_spec.md`
- 変更内容: 音高変換、スナップ、保存、進行、I/Oに対するテストケースを追加。
- 理由: 仕様に対する確認条件を先に定義するため。

- 対象: `docs/specs/08_changelog.md`
- 変更内容: 仕様変更履歴ファイルを追加。
- 理由: 仕様の変更理由と影響範囲を残すため。

- 対象: `docs/specs/dsl/01_dsl_spec.md`
- 変更内容: DSL連携仕様を本体仕様から分離し、責務・出力・root/onChord/omit方針を追加。
- 理由: DSL仕様を本体アプリ仕様と混在させないため。

## 2026-05-05 15:39:00 +09:00

- 対象: `code/index.html`, `code/styles.css`
- 変更内容: 単一ページSPAのUI骨格、音高設計画面、進行/設定/管理の各セクション、Undo/Redoボタン、数直線キャンバスを追加。
- 理由: 実装着手の土台となる画面を作るため。

- 対象: `code/src/pitch.js`
- 変更内容: `3600 microStep` 基準の変換、スナップ、A4基準周波数計算、音高正規化ロジックを追加。
- 理由: 音高処理の中核ロジックをUIから分離するため。

- 対象: `code/src/audio.js`
- 変更内容: Web Audio API を用いた発音エンジン、voice管理、短フェード停止処理を追加。
- 理由: Phase1相当の単音発音を実装するため。

- 対象: `code/src/history.js`
- 変更内容: Undo/Redo用 `HistoryManager` を追加し、履歴上限100、Redo破棄、group履歴を実装。
- 理由: 全体横断仕様として定義した履歴基盤を先に用意するため。

- 対象: `code/src/storage.js`
- 変更内容: IndexedDBを使ったプロジェクト保存/読込処理を追加。
- 理由: `localhost` 運用前提の永続保存基盤を作るため。

- 対象: `code/src/app.js`
- 変更内容: 状態管理、入力反映、数直線描画、activeNotes管理、Undo/Redo接続、エクスポート/インポート骨格を追加。
- 理由: 仕様にもとづく初期実装を動かし始めるため。

- 対象: `code/manifest.webmanifest`, `code/service-worker.js`
- 変更内容: PWA manifest と Service Worker を追加。
- 理由: 初期からPWA前提の構成にするため。

## 2026-05-05 15:39:10 +09:00

- 対象: `code/README.md`
- 変更内容: `localhost` 前提の起動方法を新規記載。`python -m http.server 5173` と `npx serve code` を案内。
- 理由: `file://` 直開きでは動作しない実装条件を明示するため。

- 対象: `code/index.html`, `code/styles.css`
- 変更内容: runtime警告バナー領域と警告表示用スタイルを追加。
- 理由: `localhost` 以外で起動した際に原因不明で止まる状態を避けるため。

- 対象: `code/src/app.js`
- 変更内容: `location.protocol` を見て runtime check を行い、`file://` 等で警告表示する処理、初期化失敗時のUIエラー表示、Service Worker登録ガードを追加。
- 理由: ES Modules / Service Worker のブラウザ制約をユーザーに明示するため。

- 対象: `docs/specs/05_ui_spec.md`
- 変更内容: `UI-RUNTIME-001` を追加し、runtime警告バナー表示仕様を明記。
- 理由: 実行環境不整合時の画面挙動を仕様化するため。

- 対象: `docs/specs/06_implementation_spec.md`
- 変更内容: `IMPL-RUNTIME-001` を追加し、開発時 `localhost` 前提の運用条件を明記。
- 理由: 実装運用前提を仕様へ反映するため。

- 対象: `docs/specs/07_test_spec.md`
- 変更内容: `TEST-RUNTIME-001..003` を追加し、`localhost` / `file://` / Service Worker登録条件の確認ケースを追加。
- 理由: 起動条件の回帰確認を可能にするため。

## 2026-05-05 15:41:22 +09:00

- 対象: `CHANGELOG.md`
- 変更内容: ルート配下の変更を必ず時刻付きで記録するための作業ログファイルを新規追加。
- 理由: 実装変更・仕様変更の追跡性を確保するため。

- 対象: `docs/specs/00_overview.md`
- 変更内容: `OVR-LOG-001` を追加し、ルート `CHANGELOG.md` へ必ず変更ログを記録する運用規約を明記。
- 理由: ログ運用自体を仕様側にも固定するため。

## 2026-05-05 15:43:46 +09:00

- 対象: `CHANGELOG.md`
- 変更内容: 既存ログを全面整理し、ここまでの仕様更新・実装着手・runtime対策・ログ運用追加を時系列で再記録。
- 理由: 文字化けしていた旧エントリを修正し、「何をどう変えたか」が追える状態に戻すため。

## 2026-05-05 15:46:40 +09:00

- 対象: `CHANGELOG.md`
- 変更内容: 既存エントリを再確認し、ここまでの仕様更新・実装着手・runtime対策・ログ運用の流れが時系列で追えることを確認したうえで、監査完了ログを追記。
- 理由: 作業履歴が残っていないという指摘に対して、現時点の全体変更履歴が追跡可能な状態であることを明示するため。

## 2026-05-05 15:57:05 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: 音高設計画面下部に音高プリセット登録UIとコード保存UIを追加し、`pitchPresets` / `chordPresets` の状態管理、一覧表示、`.mcb` への保存復元、activeNotes からの相対コード保存処理を実装。
- 理由: 実装フェーズ2として、命名済み音高の再利用と activeNotes からのコード化を可能にし、進行編集実装前の基盤を整えるため。

## 2026-05-05 16:25:47 +09:00

- 対象: `code/src/app.js`, `code/src/audio.js`, `code/styles.css`
- 変更内容: 数直線ドラッグを相対移動ベースへ修正して端越え時のオクターブ暴走を抑制し、momentary/toggle のドラッグ発音を単一ボイス更新へ変更、数直線上の発音中マーカー表示、activeNotes の個別削除UI、activeNotes の永続化除外を実装。
- 理由: ドラッグ操作時の発音品質と視認性を改善し、リロード後に activeNotes が残留増殖する不具合を解消するため。

## 2026-05-05 16:47:01 +09:00

- 対象: `code/src/app.js`
- 変更内容: 数直線上の activeNotes マーカーをオクターブごとに縦方向へオフセットして描画するよう変更し、同一 pitch class の複数オクターブ音が団子状に見分けられるように調整。
- 理由: ページ送り後も同じ横位置に重なっていた赤丸と番号を、一覧性を保ったまま視認可能にするため。

## 2026-05-05 16:53:19 +09:00

- 対象: `code/src/app.js`
- 変更内容: activeNotes マーカーの縦オフセット基準オクターブを `3` に変更。
- 理由: 3オクターブ目が縦中央に来る想定に合わせ、2オクターブ以下で十分に下方向へ分離されるようにするため。

## 2026-05-05 17:01:36 +09:00

- 対象: `code/index.html`, `code/src/app.js`
- 変更内容: コード編集領域に保存済みコード呼び出しUIを追加し、`activeNotes` へコードを展開する処理、ルート音源の `pitchDraft` / 音高プリセット切替、ルートオクターブ変更を実装。
- 理由: 実装フェーズ3として、保存済みコードの再利用とルート差し替えを可能にするため。

## 2026-05-05 17:09:48 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: コード進行画面にセル追加・選択・削除・列数変更・ループ・再生停止UIを追加し、保存済みコードを進行セルとして保持して順送り再生する最小実装を追加。
- 理由: 実装フェーズ4として、保存済みコードを並べて時間軸で試聴できる最初の一貫ループを成立させるため。

## 2026-05-05 17:28:50 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: コード進行画面を Chordbot 風の簡潔な編集UIへ再構成し、セル一覧の簡素化、下部編集パネル、ルート音の直接指定入力（例 `D#3`）、ルート文字/臨時記号/オクターブのクイック選択、コードボタン選択、`Insert/Update/Remove` 操作を実装。
- 理由: 進行画面の操作負荷を下げ、ルート指定を音高プリセット依存ではなく日常的なコード入力フローに合わせるため。

## 2026-05-05 17:49:36 +09:00

- 対象: `code/index.html`, `code/src/app.js`
- 変更内容: 進行エディタを選択セルへの即時反映方式へ変更し、`Update` ボタン依存を解消。コード一覧と進行用コードボタンにタグ/文字列フィルタ入力を追加。ルート直指定の臨時記号処理を `ROOT_ACCIDENTAL_OPTIONS` ベースへ寄せ、将来拡張しやすい形へ整理。
- 理由: 既存セルを一々更新ボタンで確定する手間を減らし、コード数増加時の選択性と今後の臨時記号拡張余地を確保するため。

## 2026-05-05 17:56:43 +09:00

- 対象: `code/index.html`, `code/src/app.js`
- 変更内容: コード進行画面でセル選択時および音程系編集時に都度コードをプレビュー発音する処理を追加。あわせて `.mcbl` の library export/import を追加し、project と library で読込経路を分岐。
- 理由: 進行編集中に音の変化を即確認できるようにし、次フェーズとして project/library I/O を分離して運用可能にするため。

## 2026-05-05 16:55:25 +09:00

- 対象: `code/src/app.js`
- 変更内容: activeNotes マーカーの縦基準位置を下端基準からキャンバス中央基準へ修正し、`octave 3` が実際に縦中央へ配置されるよう変更。
- 理由: 直前の修正では基準オクターブのみ変更しており、縦中央配置そのものが反映されていなかったため。

## 2026-05-09 16:48:17 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: コード進行画面に PC ドラッグとモバイル長押し起点の並び替えを追加し、並び替え時は選択セル維持・必要時再生停止・Undo/Redo 対応に整理。あわせてモバイルではツール群を `Menu` 折りたたみへ寄せつつ、編集パネルは `Edit` 開閉と選択時自動展開で同じ情報へ到達できるよう再構成。
- 理由: 進行画面を実用段階へ進めるため、セル順編集の完成度と狭幅端末での操作導線を先に整える必要があったため。

## 2026-05-09 17:12:00 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`, `code/manifest.webmanifest`, `code/service-worker.js`, `code/README.md`, `netlify.toml`, `code/icons/*`
- 変更内容: runtime 判定を `file:// / localhost / LAN http / https` ベースで拡張し、LAN 閲覧用案内と HTTPS 用 PWA install 導線を追加。manifest と service worker を更新し、アイコン資産と Netlify 用公開設定、モバイル/公開向け起動ガイドも整備。
- 理由: スマホ・タブレットでの LAN 閲覧と、HTTPS 公開時の PWA 利用を同じ実装基盤で成立させるため。

## 2026-05-09 21:38:16 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: pitch 画面の入力幅とレイアウトを詰め、`持続音` チェックボックス化、line readout のキャンバス内表示、Undo/Redo 横の Master Vol. スライダ追加、activeNotes 一括削除と行左の削除ボタンを追加。あわせて activeNotes 行内での音高プリセット直接追加 UI と、コード呼び出しのルート直接入力 / プリセット選択 UI に再構成。
- 理由: プリセット登録とコード呼び出し周辺の操作負荷を下げ、端末幅に応じて無駄なく扱える UI に寄せるため。

## 2026-05-09 22:04:00 +09:00

- 対象: `code/index.html`
- 変更内容: 破損していた閉じタグと文字化け混じりのマークアップを全面的に正常な HTML 構造へ復旧し、`Pitch / Progression / Settings / Manage` 各タブと `pitchLine` キャンバスが確実に DOM 上へ載る状態に修正。
- 理由: DOM 構造の崩れにより数直線描画とタブ切替イベント登録が巻き込まれて停止していたため、まず HTML を正規化して依存要素を安定化させる必要があった。

## 2026-05-09 22:21:30 +09:00

- 対象: `code/src/app.js`
- 変更内容: `renderActiveNotes` / `renderRecallSummary` / `resolveRecallRoot` / `saveCurrentPitchPreset` / `populateRecallRootPresetOptions` 周辺に残っていた旧 UI 実装の重複定義を整理し、`activeNotes` 行の DOM 生成を文字列 `innerHTML` 依存から明示的な要素生成へ置換。
- 理由: 古い関数定義が後勝ちで残って新 UI の DOM 前提と衝突しており、数直線描画やタブ切替まで巻き込んで初期化が不安定になっていたため。

## 2026-05-09 23:05:00 +09:00

- 対象: `code/index.html`, `code/src/app.js`, `code/README.md`, `docs/specs/*`
- 変更内容: UI の静的文言を日本語へ統一し、runtime / PWA / activeNotes / コード呼び出し周辺の表示文言も整理。あわせて文字化けしていた README と仕様書群を UTF-8 の現行実装ベース内容へ全面更新。
- 理由: 表示文字化けと英語残りにより操作理解と仕様参照が難しくなっていたため、実装と文書の両方を現状へ揃え直す必要があった。

## 2026-05-09 23:56:46 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: ヘッダ内へナビゲーションを移し、数直線の高さを縮小し、readout を右下へ移動。音高入力幅を詰めて `持続音` をインライン表示へ変更し、`activeNotes` と音高プリセットを左右配置に再構成。音高 / コードの保存済み一覧はテーブル表示へ置き換え、音高プリセットはクリック追加、各プリセットはダブルクリック編集に対応。あわせて cent 表示の末尾ゼロ省略と関連文言の整理を実施。
- 理由: 入力とプリセット操作の視認性がまだ低く、横幅の使い方と一覧性、再利用導線をさらに実用寄りへ寄せる必要があったため。

## 2026-05-10 21:45:38 +09:00

- 対象: `code/index.html`, `code/src/app.js`
- 変更内容: 文字化けしたマークアップ断片が残っていた `index.html` を全面的に正規化し、ヘッダ・各タブ・音高/コード/入出力 UI の文言を再定義。`app.js` では runtime 判定、PWA 案内、activeNotes、音高プリセット保存、コード保存/呼び出し、進行サマリなど実際に画面へ出る関数群を後勝ちで再定義し、旧文字列や中途半端な差し込みの影響を切った。
- 理由: 前回調整で旧定義と新定義が混在したままになっており、表示と依存関係が一貫していなかったため、動作系を壊さずに画面文言と UI ルートをまとめて正常系へ戻す必要があった。

- 対象: `code/src/app.js`
- 変更内容: `localhost` / LAN / 非 HTTPS では既存 Service Worker 登録と Cache Storage を起動時に自動解除するよう変更し、HTTPS 公開時のみ Service Worker を再登録する運用に変更。
- 理由: 開発中に古い `index.html` / `app.js` が PWA キャッシュから返り続け、修正後も UI とイベントが更新されない状態が起きていたため。

- 対象: `code/src/app.js`
- 変更内容: `classifyRuntime`、`renderPwaPrompt`、`populateChordBaseRootOptions`、`syncFormFromState`、`render` など重複していた同名関数の旧ブロックを除去し、各関数を 1 定義へ整理。
- 理由: ES module として読み込む際に `Identifier 'populateChordBaseRootOptions' has already been declared` でパースが停止し、数直線描画やタブ切替を含む初期化全体が失敗していたため。

- 対象: `code/src/app.js`
- 変更内容: 重複整理時に欠落していた `toggleCurrentNote`、`clearAllActiveNotes`、`addPitchPresetToActiveNotes` を復旧し、数直線確定時の保持、activeNotes 一括削除、音高プリセットからの追加処理を再接続。
- 理由: `activeNotes` 保持やプリセット追加時に未定義参照で停止し、持続音は出るが保持されない状態になっていたため。

## 2026-05-10 22:29:18 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: `入出力` を `設定` タブ内へ統合し、各タブ見出しを外してナビボタンを大型化。全体の横余白、入力幅、一覧テーブル、音高プリセット周辺のグリッド配分を詰めて画面占有率を調整。あわせて音高プリセットとコード一覧の編集をポップアップ式から行内インライン編集へ変更し、音高プリセットはダブルクリック時に単クリック追加が発火しないよう遅延処理へ切り替えた。
- 理由: 画面全体の利用効率がまだ低く、プリセット修正時の操作衝突と編集体験が実用水準に達していなかったため。

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: 音高側を `activeNotes` と `音高プリセット` の上下統合ビューへ再編し、それぞれ独立スクロール化。`activeNotes` もプリセット一覧と同系統のテーブル表示へ変更。コード側も保存フォーム・基音指定・一覧を 1 パネルへ統合し、コード一覧はシングルクリックで呼び出し、ダブルクリックでインライン編集に分岐するよう更新。コード一覧の `基準: oct / ms` 表示は廃止し、一覧上は現在の基音入力を表示する形へ変更。
- 理由: 画面上で同列に扱いたい情報が分断されており、表示幅の使い方とコード呼び出し導線がまだ不自然だったため。

## 2026-05-11 00:00:08 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: 音高一覧をさらに表形式へ寄せ、`activeNotes` 側でも cent を直接編集できるよう更新。タグ表示は色付きチップ化。コード呼び出し時に無音になっていた原因である `loadChordIntoActiveNotes` の欠落を復旧し、コード一覧のシングルクリック呼び出し経路を再接続。数直線内ラベルの位置とサイズも調整して、高さを維持したまま潰れにくくした。
- 理由: 基音付きコード呼び出しが実行されず、一覧統合後も見た目と編集導線が要求水準に届いていなかったため。

## 2026-05-11 00:21:33 +09:00

- 対象: `code/index.html`, `code/styles.css`, `code/src/app.js`
- 変更内容: 音高上部の入力帯を `pitch-toolbar` 化して横幅を使う配置へ変更。`activeNotes` / 音高プリセット / コードプリセットの表スタイルを罫線ベースへ寄せ、タグと構成音をチップ表示化。コード一覧は先頭に追加行を持つ表形式へ再構成し、旧入力フォームを非表示化。コード単クリック時は hidden select 依存をやめ、対象コード ID を直接 `loadChordIntoActiveNotes` へ渡して展開・発音するよう修正。あわせて音高プリセットの cent 編集とコード構成音のインライン編集を追加。
- 理由: コード呼び出しが無音になる経路が残っており、一覧 UI もまだ旧フォーム依存で表形式の要求を満たしていなかったため。
